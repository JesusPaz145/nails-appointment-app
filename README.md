# Nails Appointment App

Aplicación para la gestión de citas en una estética de uñas.

## 🚀 Estructura del Proyecto

- `app/`: Contiene el código principal de la aplicación (Python/FastAPI).
- `Dockerfile`: Configuración para contenedores Docker.
- `docker-compose.yml`: Orquestación de servicios (App y BD).

## 🧞 Comandos

Para ejecutar la aplicación localmente con Docker:

```sh
docker-compose up --build
```

Para instalar dependencias locales:

```sh
pip install -r requirements.txt
```
