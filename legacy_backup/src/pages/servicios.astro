---
import Layout from '../layouts/Layout.astro';
import AppointmentBooking from '../components/AppointmentBooking.jsx';
import { api } from '../utils/api'; // We can use this on server side too if we fetch in frontmatter? 
// No, api.js uses browser fetch and relative URLs? 
// Wait, for SSR/SSG fetching in Astro frontmatter, we need absolute URL or use standard fetch if we are in node context.
// But we are in "static" mode by default unless adapter is used. 
// "create astro --minimal" is usually static.
// But we need dynamic data.
// We should probably fetch on client side for now to be safe, or configure server output.
// Let's use client side fetching for simplicity as the backend runs separately.
---

<Layout title="Nuestros Servicios">
    <div class="container" style="padding: 4rem 1rem;">
        <h1 class="text-center" style="color: var(--color-primary); margin-bottom: 3rem;">Nuestros Servicios y Reservas</h1>
        
        <!-- We mount the booking component directly here. 
             It handles fetching services and displaying them in the Step 1 grid. -->
        <AppointmentBooking client:load />
    </div>
</Layout>

<script>
    // No extra script needed, component handles it
</script>
