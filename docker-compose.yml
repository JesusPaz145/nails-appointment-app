version: '3.8'

services:
  web:
    build: .
    command: python run.py
    ports:
      - "5000:5000"
    environment:
      # Conexión a Base de Datos Externa (192.168.4.175)
      - DB_HOST=192.168.4.175
      - DB_PORT=5433
      - DB_USER=admin
      - DB_PASS=Yisus78_Secure!db26
      - DB_NAME=nailsbyanais
    # No depends_on, as DB is external